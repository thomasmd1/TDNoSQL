<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1>Hello World</h1>
        <ul>
        <% persons.forEach(function(p) { %>
            <li><%= p.firstname + ' ' + p.lastname %></li>
        <% }); %>
        </ul>
    </body>
</html>
